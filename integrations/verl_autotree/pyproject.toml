[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "verl-autotree"
version = "0.1.0"
description = "AutoTree rollout adapters for verl"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["verl_autotree"]
